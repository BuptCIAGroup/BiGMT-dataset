# BiGMT-dataset 数据说明

## 数据存放
本项目的核心数据文件存放于 `./data/` 目录下，包含筛选后的小于1GB的数据集文件（如 `.vti` 格式的各类三维数据文件）。

## 完整数据集获取
由于部分大数据文件（大于1GB）未包含在本目录中，如需获取完整数据集，可访问 Kaggle 官方数据集地址：
> https://www.kaggle.com/datasets/wh1stle/bigmt-dataset

## 数据格式说明
- 主要文件格式：`.vti`（VTK Image Data，用于存储三维标量/矢量数据）
